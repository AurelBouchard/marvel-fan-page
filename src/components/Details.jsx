import React from 'react';

/**
 * Show some data of the selected item :
 * name, photo,
 *
 * @param searchResult
 * @returns {JSX.Element}
 * @constructor
 */
export default function Details({searchResult}) {
    
    return (
        <div className="flex-auto w-36 bg-dark rounded-xl p-4">
        details
        </div>
    )
}